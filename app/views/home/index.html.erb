<h3>Home</h3>

<p>
<% if user_signed_in? %>
	<% if current_user.admin? %>
    	ADMIN !!
    	<% else %>
    	not admin :(
	<% end %>
<% end %>

---

<% if policy(User).index? %>
    <li><%= link_to 'Users', users_path %></li>
    <% else %> 
    not admin, sorry
<% end %>

</p>

<p>
<%= @identities.inspect %>
</p>
<p>
<%= current_user.inspect %> 
</p>

<h3>public models &amp; methods</h3>
<ul>
	<li><%= link_to 'Castles', castles_path %></li>
		
	<li><%= link_to 'Photos', photos_path %></li>
	<li><%= link_to 'Resources', resources_path %></li>
</ul>