<h1>Listing kokuins</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @kokuins.each do |kokuin| %>
      <tr>
        <td><%= kokuin.id %>
        <td><%= kokuin.shortDesc %>
        <td><%= link_to 'Show', kokuin %></td>
        <td><%= link_to 'Edit', edit_kokuin_path(kokuin) %></td>
        <td><%= link_to 'Destroy', kokuin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kokuin', new_kokuin_path %>
